# Pinned PyPI package versions for reproducible Pyodide builds (the Python engines install these
# at runtime via micropip). Resolved under Pyodide 0.26.4 / CPython 3.12. Bump deliberately, then
# re-run `make reference-capture` to re-baseline.
#
# FAdo and its lark/bitarray/deprecation runtime deps install with deps=false: FAdo's full
# dependency set (z3-solver, jupyter, networkx, ...) has no Pyodide wheels, but FAdo.reex needs
# only these three.
regex==2024.4.16
greenery==4.2.2
interegular==0.3.3
automata-lib==9.2.0
pyformlang==1.0.11
fado==2.2.0
lark==1.3.1
bitarray==3.9.1
deprecation==2.1.0
