[package]
name = "rust_regex_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
regex = "1"
regress = "0.11"
fancy-regex = "0.18"
regex-lite = "0.1"
resharp = "0.6"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
